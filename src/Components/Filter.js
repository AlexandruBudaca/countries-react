import React from "react";

const Filter = () => {
  return (
    <div className="filter">
      <select>
        <option selected disabled hidden>
          Filter by Origin
        </option>
        <option></option>
      </select>
    </div>
  );
};

export default Filter;
